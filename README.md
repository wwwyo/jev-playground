# jev-playground

TypeSafe AI の Jev（型付きの判断と確率を返す model）を手元のデータで試す playground。

## Getting Started

前提: mise、`TYPESAFE_API_KEY`（mise + age で管理）。

```bash
mise install
bun install
bun run examples/triage.ts
```
